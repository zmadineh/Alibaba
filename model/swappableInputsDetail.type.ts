import {data} from "./data.type";

export interface swappableInputsDetailType {
    name: string,
    label: string,
    subLabel: string,
    data: data[],
    iconName: string,
    listDescription: boolean,
}
